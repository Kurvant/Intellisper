---
title: 'Environment Variables'
description: ''
---

To configure Intellisper, you will need to set some environment variables, There is file called `.env` at the root directory for our main repo.

<Tip> When you execute the [tools/deploy.sh](https://github.com/Kurvant/Intellisper/blob/main/tools/deploy.sh) script in the Docker installation tutorial, 
it will produce these values. </Tip>

## Environment Variables

| Variable                           | Description                                                                                                                                                                                                          | Default Value                                                                  | Example                                                                |
| ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | ---------------------------------------------------------------------- |
| `IB_CONFIG_PATH`                   | Optional parameter for specifying the path to store PGLite database and local settings.                                                                                                                                      | `~/.intellisper`                                                              |                                                                        |
| `IB_CLOUD_AUTH_ENABLED`            | Turn off the utilization of Intellisper oauth2 applications                                                                                                                                                         | `true`                                                                        |                                                                        |
| `IB_DB_TYPE`                       | The type of database to use. `POSTGRES` for external PostgreSQL, `PGLITE` for embedded database. **Note:** `SQLITE3` is deprecated and will be automatically migrated to `PGLITE`.                                                                                                                                                                    | `POSTGRES`                                                                      |                                                                        |
| `IB_EXECUTION_MODE`                | You can choose between 'SANDBOX_PROCESS', 'UNSANDBOXED', 'SANDBOX_CODE_ONLY', 'SANDBOX_CODE_AND_PROCESS' as possible values. If you decide to change this, make sure to carefully read https://intellisper.com/docs/install/architecture/sandboxing                                                                | `UNSANDBOXED`                                                                  |                                                                        |
| `IB_ENCRYPTION_KEY`                | ❗️ Encryption key used for connections is a 32-character (16 bytes) hexadecimal key. You can generate one using the following command: `openssl rand -hex 16`.                                                                                                                                                                              | None                                                                         |
| `IB_EXECUTION_DATA_RETENTION_DAYS` | The number of days to retain execution data, logs and events.                                                                                                                                                        | `30`                                                                         |                                                                        |
| `IB_FRONTEND_URL`                  | ❗️ Url that will be used to specify redirect url and webhook url. 
| `IB_INTERNAL_URL`                  | (BETA) Used to specify the SSO authentication URL.                                                                                 | None                                                                         | [https://demo.intellisper.com/api](https://demo.intellisper.com/api) |
| `IB_JWT_SECRET`                    | ❗️ Encryption key used for generating JWT tokens is a 32-character hexadecimal key. You can generate one using the following command: `openssl rand -hex 32`.                                                                                                                                                                     | None                                                                         | [https://demo.intellisper.com](https://demo.intellisper.com)         |
| `IB_QUEUE_UI_ENABLED`              | Enable the queue UI (only works with redis)                                                                                                                                                                          | `false`                                                                         |                                                                        |
| `IB_QUEUE_UI_USERNAME`             | The username for the queue UI. This is required if `IB_QUEUE_UI_ENABLED` is set to `true`.                                                                                                                           | None                                                                           |                                                                        |
| `IB_QUEUE_UI_PASSWORD`             | The password for the queue UI. This is required if `IB_QUEUE_UI_ENABLED` is set to `true`.                                                                                                                           | None                                                                           |                                                                        |
| `IB_REDIS_FAILED_JOB_RETENTION_DAYS` | The number of days to retain failed jobs in Redis.                                                                                                                                                                  | `30`                                                                           |                                                                        |
| `IB_REDIS_FAILED_JOB_RETENTION_MAX_COUNT` | The maximum number of failed jobs to retain in Redis.                                                                                                                                                          | `100000`                                                                         |                                                                        |
| `IB_TRIGGER_DEFAULT_POLL_INTERVAL` | How many minutes before the system checks for new data updates for blocks with scheduled triggers, such as new Google Contacts.                                                       | `5`                                                                            |                                                                        |
| `IB_BLOCKS_SYNC_MODE`              | How official blocks are sourced: `NONE` (no metadata syncing — local DB only), `OFFICIAL_AUTO` (sync from `IB_BLOCKS_REGISTRY_URL`), or `NPM` (resolve from the public NPM registry / bundled blocks). This edition ships no hardcoded registry, so the default makes no outbound block call. | `NONE`                                                                |
| `IB_BLOCKS_REGISTRY_URL`           | Registry host used **only** when `IB_BLOCKS_SYNC_MODE=OFFICIAL_AUTO`. There is no hardcoded default in this edition — point it at your own registry (e.g. `https://registry.example.com/api/v1/blocks`). Leave empty to disable sync. | None                                                                |
| `IB_POSTGRES_DATABASE`             | ❗️ The name of the PostgreSQL database                                                                                                                                                                              | None                                                                         |                                                                        |
| `IB_POSTGRES_HOST`                 | ❗️ The hostname or IP address of the PostgreSQL server                                                                                                                                                              | None                                                                         |                                                                        |
| `IB_POSTGRES_PASSWORD`             | ❗️ The password for the PostgreSQL,  you can generate a 32-character hexadecimal key using the following command: `openssl rand -hex 32`.                                                                                                                                                                              | None                                                                         |                                                                        |
| `IB_POSTGRES_PORT`                 | ❗️ The port number for the PostgreSQL server                                                                                                                                                                        | None                                                                         |                                                                        |
| `IB_POSTGRES_USERNAME`             | ❗️ The username for the PostgreSQL user                                                                                                                                                                             | None                                                                         |                                                                        |
| `IB_POSTGRES_USE_SSL`              | Use SSL to connect the postgres database                                                                                                                                                                             | `false`                                                                        |                                                                        |
| `IB_POSTGRES_SSL_CA`               | Use SSL Certificate to connect to the postgres database                                                                                                                                                              |
| `IB_POSTGRES_URL`                  | Alternatively, you can specify only the connection string (e.g postgres://user:password@host:5432/database) instead of providing the database, host, port, username, and password.                                   | None                                                                         |                                                                        |
| `IB_POSTGRES_POOL_SIZE` | Maximum number of clients the pool should contain for the PostgreSQL database  |  None
| `IB_POSTGRES_IDLE_TIMEOUT_MS` | Sets the idle timout  pool for your PostgreSQL  | `300000`
| `IB_REDIS_TYPE`                    | Where to spin redis instance, either in memory (MEMORY) or in a dedicated instance (STANDALONE), or in a sentinel instance (SENTINEL)                                                                                  | `STANDALONE`                                                                       |                                                                        |
| `IB_REDIS_URL`                     | If a Redis connection URL is specified, all other Redis properties will be ignored.                                                                                                                                  | None                                                                         |                                                                        |
| `IB_REDIS_USER`                    | ❗️ Username to use when connect to redis                                                                                                                                                                            | None                                                                         |                                                                        |
| `IB_REDIS_PASSWORD`                | ❗️ Password to use when connect to redis                                                                                                                                                                            | None                                                                         |                                                                        |
| `IB_REDIS_HOST`                    | ❗️ The hostname or IP address of the Redis server                                                                                                                                                                   | None                                                                         |                                                                        |
| `IB_REDIS_PORT`                    | ❗️ The port number for the Redis server                                                                                                                                                                             | None                                                                         |                                                                        |
| `IB_REDIS_DB`                      | The Redis database index to use                                                                                                                                                                                      | `0`                                                                            |                                                                        |
| `IB_REDIS_USE_SSL`                 | Connect to Redis with SSL                                                                                                                                                                                            | `false`                                                                        |                                                                        |
| `IB_REDIS_SSL_CA_FILE`           | The path to the CA file for the Redis server.                                                                                                                                                             | None |  |
| `IB_REDIS_SENTINEL_HOSTS`          | If specified, this should be a comma-separated list of `host:port` pairs for Redis Sentinels. Make sure to set `IB_REDIS_CONNECTION_MODE` to `SENTINEL`                                                                 | None | `sentinel-host-1:26379,sentinel-host-2:26379,sentinel-host-3:26379` |
| `IB_REDIS_SENTINEL_NAME`          | The name of the master node monitored by the sentinels.                                                                                                                                                             | None | `sentinel-host-1` |
| `IB_REDIS_SENTINEL_ROLE`          | The role to connect to, either `master` or `slave`.                                                                                                                                                             | None | `master` |
| `IB_TRIGGER_TIMEOUT_SECONDS`       | Maximum allowed runtime for a trigger to perform polling in seconds                                                                                                                                                                     | `60`                                                                         |                                                                        |
| `IB_FLOW_TIMEOUT_SECONDS`          | Maximum allowed runtime for a flow to run in seconds                                                                                                                                                                        | `600`                                                                         |                                                                        |
| `IB_SANDBOX_MEMORY_LIMIT`          | The maximum amount of memory (in kilobytes) that a single sandboxed engine process can use. Each engine process executes at most one execution at a time. This helps prevent runaway memory usage in custom code or blocks. If not set, the default is 1,048,576 KB (1,024 MB).                    | `1048576`                                                                         | `1048576`                                                                 |
| `IB_SANDBOX_PROPAGATED_ENV_VARS`   | Environment variables that will be propagated to the sandboxed code. If you are using it for blocks, we strongly suggests keeping everything in the authentication object to make sure it works across AP instances. | None                                                                         |                                                                        |
| `IB_SCIM_DEFAULT_PROJECT_ROLE`     | The default project role assigned to members when they are added to a project via SCIM group sync. Accepted values: `Admin`, `Editor`, `Viewer`.                                                                     | `Editor`                                                                       | `Viewer`                                                               |
| `IB_TELEMETRY_ENABLED`             | **No external telemetry relay exists in this edition** — Segment/PostHog and template-telemetry relay code has been removed. This flag now only gates local debug logging of captured events; it never sends data anywhere. Kept for compatibility. | `false`                                                                         |                                                                        |
| `IB_TEMPLATES_SOURCE_URL`          | Base URL the community template marketplace fetches from. No hardcoded Intellisper host in this edition: leave empty to disable the marketplace (no outbound request, empty template list), or set it to your own host (e.g. `https://templates.example.com/api/v1/flow-templates`). | None                         |                                                                        |
| `IB_WEBHOOK_TIMEOUT_SECONDS`       | The default timeout for webhooks. The maximum allowed is 15 minutes. Please note that Cloudflare limits it to 30 seconds. If you are using a reverse proxy for SSL, make sure it's configured correctly.             | `30`                                                                           |                                                                        |
| `IB_PROJECT_RATE_LIMITER_ENABLED`   | Enforce rate limits and prevent excessive usage by a single project.                                                                                                                                                   | `false`                                                                           |                                                                        |
| `IB_DEFAULT_CONCURRENT_JOBS_LIMIT`| The default maximum number of concurrent runs a project can have. Used to enforce rate limits and prevent excessive usage by a single project. Can be overridden per-project in settings.                                                                                 | `5`                                                                           |                                                                        |
| `IB_S3_ACCESS_KEY_ID`              | The access key ID for your S3-compatible storage service. Not required if `IB_S3_USE_IRSA` is `true`.                                                                                                                | None                                                                         |                                                                        |
| `IB_S3_SECRET_ACCESS_KEY`          | The secret access key for your S3-compatible storage service. Not required if `IB_S3_USE_IRSA` is `true`.                                                                                                            | None                                                                         |                                                                        |
| `IB_S3_BUCKET`                     | The name of the S3 bucket to use for file storage.                                                                                                                                                                   | None                                                                         |                                                                        |
| `IB_S3_ENDPOINT`                   | The endpoint URL for your S3-compatible storage service. Not required if `AWS_ENDPOINT_URL` is set.                                                                                                                  | None                                                                         | `https://s3.amazonaws.com`                                             |
| `IB_S3_REGION`                     | The region where your S3 bucket is located. Not required if `AWS_REGION` is set.                                                                                                                                     | None                                                                         | `us-east-1`                                                            |
| `IB_S3_USE_SIGNED_URLS`            | Routes file traffic directly to S3 using pre-signed URLs, bypassing the API server. The bucket should remain private; signed URLs provide temporary authenticated access.                                             | None                                                                         |                                                                        |
| `IB_S3_USE_IRSA`                   | Use IAM Role for Service Accounts (IRSA) to connect to S3. When `true`, `IB_S3_ACCESS_KEY_ID` and `IB_S3_ACCESS_KEY_ID` are not required.                                                                            | None                                                                         |  `true`                                                                |
| `IB_SMTP_HOST`                     | The host name for the SMTP server that Intellisper uses to send emails                                                                                                                                              | `None`                                                                         |  `mail.example.com`                                                    |
| `IB_SMTP_PORT`                     | The port number for the SMTP server that Intellisper uses to send emails                                                                                                                                            | `None`                                                                         |  587                                                                   |
| `IB_SMTP_USERNAME`                 | The user name for the SMTP server that Intellisper uses to send emails                                                                                                                                              | `None`                                                                         |  test@mail.example.com                                                 |
| `IB_SMTP_PASSWORD`                 | The password for the SMTP server that Intellisper uses to send emails                                                                                                                                               | `None`                                                                         |  secret1234                                                            |
| `IB_SMTP_SENDER_EMAIL`                | The email address from which Intellisper sends emails.                                                                                                                                                              | `None`                                                                         |  test@mail.example.com                                                 |
| `IB_SMTP_SENDER_NAME`                 | The sender name Intellisper uses to send emails.   
| `IB_MAX_FILE_SIZE_MB`              | The maximum allowed file size (in megabytes) for **uploaded files** in steps or triggers. Files larger than this value will be rejected. This does **not** control flow run log size—see `IB_MAX_FLOW_RUN_LOG_SIZE_MB`.  | `25`                                                                         | `10`                                                                   |
| `IB_MAX_FLOW_RUN_LOG_SIZE_MB`      | The maximum allowed size (in megabytes) of the **flow run logs**—this is the total combined size of all inputs and outputs for each step in a single flow run. If logs exceed this size, they will be truncated, which may cause flow execution issues. | `50`                                                                         | `25`                                                                   |
| `IB_FLOW_RUN_LOG_SLICE_THRESHOLD_KB` | Step outputs whose size exceeds this threshold (in KB) are offloaded as `FLOW_RUN_LOG_SLICE` files to object storage instead of being inlined in the flow run log. Lowering the value reduces worker memory pressure on runs with large step outputs; raising it reduces the number of object-storage requests per run. | `32` | `32` |
| `IB_FLOW_RUN_LOG_INPUT_TRUNCATE_THRESHOLD_KB` | Step input values whose serialized size exceeds this threshold (in KB) are replaced with a placeholder string (`(truncated, original size <N>)`) before being stored in the flow run log. Source data still lives in the upstream step's output, which is recoverable when sliced. | `2` | `2` |
| `IB_MAX_WEBHOOK_PAYLOAD_SIZE_MB`   | Maximum webhook payload size in MB. Payloads exceeding this are rejected with HTTP 413. | `25` | `5` |
| `IB_WEBHOOK_PAYLOAD_INLINE_THRESHOLD_KB` | Webhook payloads below this size (in KB) are stored inline in Redis for fastest processing. Payloads above this are offloaded to file storage to protect Redis memory. | `512` | `1024` |
| `IB_WORKER_CONCURRENCY` | The number of concurrent jobs the worker can process simultaneously. Each concurrent job uses one sandbox instance. | `5` | `10` |
| `IB_FILE_STORAGE_LOCATION`         | The location to store files. Possible values are `DB` for storing files in the database or `S3` for storing files in an S3-compatible storage service.                                                               | `DB`                                                                           |                                                                        |
| `IB_PAUSED_FLOW_TIMEOUT_DAYS`      | The maximum allowed pause duration in days for a paused flow, please note it can not exceed `IB_EXECUTION_DATA_RETENTION_DAYS`                                                                                                                                                            | `30`                                                                           |                                                                        
| `IB_MAX_RECORDS_PER_TABLE`         | The maximum allowed number of records per table                                                                | `10000` | `10000`
| `IB_MAX_FIELDS_PER_TABLE` |  The maximum allowed number of fields per table  | `100` | `100`
| `IB_MAX_MCPS_PER_PROJECT` | The maximum allowed number of mcp per project | `20` | `20`
| `IB_ENABLE_FLOW_ON_PUBLISH` | Whether publishing a new flow version should automatically enable the flow | `true` | `false`  
| `IB_ISSUE_ARCHIVE_DAYS`            | Controls the automatic archival of issues in the system. Issues that have not been updated for this many days will be automatically moved to an archived state.| `7`  | `1`
| `IB_LOAD_TRANSLATIONS_FOR_DEV_BLOCKS`  | Load translations for dev blocks (configured via `IB_DEV_BLOCKS`). When disabled, dev blocks are loaded without translations. This only affects development mode.| `false`  | `true`
| `IB_CONTAINER_TYPE` | Controls which services to run in the Docker container. `APP` starts only the API server, `WORKER` starts only the worker, `WORKER_AND_APP` starts both. | `WORKER_AND_APP` | `APP`
| `IB_NETWORK_MODE` | Network posture for user code egress. `STRICT` blocks outbound connections from user code (Code steps, blocks, HTTP requests) to private, loopback, link-local, and cloud metadata IPs across every Node egress path (`axios`, `fetch`, `undici`, raw `http`/`net`); in sandboxed execution modes it also applies a kernel-level iptables lockdown. `UNRESTRICTED` disables all guards. Defaults to `UNRESTRICTED` while the hardening stack is being validated. | `UNRESTRICTED` | `STRICT`
| `IB_SSRF_ALLOW_LIST` | Comma-separated IPs or CIDR ranges that bypass `IB_NETWORK_MODE=STRICT`. Use this to reach specific internal services (databases, sidecars) from flows. Accepts exact IPs (`10.0.0.5`) and CIDR blocks (`10.0.0.0/24`, `fd00::/8`). Only applies when `IB_NETWORK_MODE=STRICT`. | `` | `10.0.0.5,10.10.0.0/24`
| `HTTP_PROXY` / `HTTPS_PROXY` | Standard proxy env vars. When set, the engine routes all outbound HTTP(S) through the proxy (via `undici`'s `EnvHttpProxyAgent`). If the proxy listens on loopback, its port is automatically exempted from the SSRF guard so user code can still reach it. | `` | `http://127.0.0.1:3128`



<Warning>
  The frontend URL is essential for webhooks and app triggers to work. It must
  be accessible to third parties to send data.
</Warning>


### Setting Webhook (Frontend URL):

The default URL is set to the machine's IP address. To ensure proper operation, ensure that this address is accessible or specify an `IB_FRONTEND_URL` environment variable.

One possible solution for this is using a service like ngrok ([https://ngrok.com/](https://ngrok.com/)), which can be used to expose the frontend port (4200) to the internet.

### Redis Configuration

Set the `IB_REDIS_URL` environment variable to the connection URL of your Redis server. 

Please note that if a Redis connection URL is specified, all other **Redis properties** will be ignored.

<Info>
If you don't have the Redis URL, you can use the following command to get it. You can use the following variables:

- `REDIS_USER`: The username to use when connecting to Redis.
- `REDIS_PASSWORD`: The password to use when connecting to Redis.
- `REDIS_HOST`: The hostname or IP address of the Redis server.
- `REDIS_PORT`: The port number for the Redis server.
- `REDIS_DB`: The Redis database index to use.
- `REDIS_USE_SSL`: Connect to Redis with SSL.
</Info>
<Info>
  If you are using **Redis Sentinel**, you can set the following environment variables:
  - `IB_REDIS_TYPE`: Set this to `SENTINEL`.
  - `IB_REDIS_SENTINEL_HOSTS`: A comma-separated list of `host:port` pairs for Redis Sentinels. When set, all other Redis properties will be ignored.
  - `IB_REDIS_SENTINEL_NAME`: The name of the master node monitored by the sentinels.
  - `IB_REDIS_SENTINEL_ROLE`: The role to connect to, either `master` or `slave`.
  - `IB_REDIS_PASSWORD`: The password to use when connecting to Redis.
  - `IB_REDIS_USE_SSL`: Connect to Redis with SSL.
  - `IB_REDIS_SSL_CA_FILE`: The path to the CA file for the Redis server.
</Info>

### SMTP Configuration

SMTP can be configured both from the platform admin screen and through environment variables. The enviroment variables are only used if the platform admin screen has no email configuration entered.

Intellisper will only use the configuration from the environment variables if `IB_SMTP_HOST`, `IB_SMTP_PORT`, `IB_SMTP_USERNAME` and `IB_SMTP_PASSWORD` all have a value set. TLS is supported.
