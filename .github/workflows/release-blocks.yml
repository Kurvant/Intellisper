name: Release Blocks

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'packages/blocks/**'
      - 'packages/shared/**'

concurrency:
  group: release-blocks
  cancel-in-progress: false

jobs:
  Release-Blocks:
    if: github.repository == 'Kurvant/Intellisper'
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: ~/.bun/install/cache
          key: bun-${{ hashFiles('bun.lock', 'package.json') }}
          restore-keys: bun-

      - name: Setup nodejs
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: copy project .npmrc to user level
        run: cp .npmrc $HOME/.npmrc

      - name: publish shared package
        run: npx turbo run build --filter=@intelblocks/shared --force && npx ts-node -r tsconfig-paths/register -P packages/server/engine/tsconfig.lib.json tools/scripts/utils/publish-npm-package.ts packages/shared 
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: publish pieces-common package
        run: npx turbo run build --filter=@intelblocks/blocks-common --force && npx ts-node -r tsconfig-paths/register -P packages/server/engine/tsconfig.lib.json tools/scripts/utils/publish-npm-package.ts packages/blocks/common
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: publish pieces-framework package
        run: npx turbo run build --filter=@intelblocks/blocks-framework --force && npx ts-node -r tsconfig-paths/register -P packages/server/engine/tsconfig.lib.json tools/scripts/utils/publish-npm-package.ts packages/blocks/framework
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Find changed pieces
        id: changed
        run: |
          OUTPUT=$(npx ts-node -r tsconfig-paths/register -P packages/server/engine/tsconfig.lib.json tools/scripts/blocks/find-changed-blocks.ts)

          TURBO_FILTER=$(echo "$OUTPUT" | grep '^TURBO_FILTER:' | sed 's/^TURBO_FILTER://')
          CHANGED_DIRS=$(echo "$OUTPUT" | sed -n '/^CHANGED_DIRS:$/,/^TURBO_FILTER:/{ /^CHANGED_DIRS:$/d; /^TURBO_FILTER:/d; p; }')

          if [ -z "$TURBO_FILTER" ]; then
            echo "No changed pieces found"
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
          else
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
            echo "turbo_filter=$TURBO_FILTER" >> "$GITHUB_OUTPUT"
            {
              echo "changed_dirs<<PIECES_EOF"
              echo "$CHANGED_DIRS"
              echo "PIECES_EOF"
            } >> "$GITHUB_OUTPUT"
            echo "Changed pieces: $TURBO_FILTER"
          fi

      - name: Build changed pieces
        if: steps.changed.outputs.has_changes == 'true'
        run: npx turbo run build ${{ steps.changed.outputs.turbo_filter }} --force

      - name: Prepare pieces for publish
        if: steps.changed.outputs.has_changes == 'true'
        run: npx ts-node -r tsconfig-paths/register -P packages/server/engine/tsconfig.lib.json tools/scripts/blocks/prepare-blocks-for-publish.ts
        env:
          CHANGED_PIECES: ${{ steps.changed.outputs.changed_dirs }}

      - name: publish pieces packages
        if: steps.changed.outputs.has_changes == 'true'
        run: npx ts-node -r tsconfig-paths/register -P packages/server/engine/tsconfig.lib.json tools/scripts/blocks/publish-blocks-to-npm.ts
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          CHANGED_PIECES: ${{ steps.changed.outputs.changed_dirs }}

      - name: Check available memory
        run: free -h

      - name: update pieces metadata
        if: steps.changed.outputs.has_changes == 'true'
        run: npx ts-node -r tsconfig-paths/register -P packages/server/engine/tsconfig.lib.json tools/scripts/blocks/update-blocks-metadata.ts packages/blocks/framework
        env:
          IB_CLOUD_API_KEY: ${{ secrets.IB_CLOUD_API_KEY }}
          NODE_OPTIONS: "--max-old-space-size=8192"
          CHANGED_PIECES: ${{ steps.changed.outputs.changed_dirs }}

      - name: Notify Discord on failure
        if: failure()
        run: |
          curl -H "Content-Type: application/json" \
            -X POST \
            -d "{\"content\": \"🚨 **Release Pieces Workflow Failed** 🚨\\n\\n**Repository:** ${{ github.repository }}\\n**Branch:** ${{ github.ref_name }}\\n**Commit:** ${{ github.sha }}\\n**Action URL:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\\n\\nThe release pieces workflow has failed. Please check the logs for more details.\"}" \
            ${{ secrets.DISCORD_ON_CALL_WEBHOOK }}
