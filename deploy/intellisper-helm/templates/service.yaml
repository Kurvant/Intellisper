apiVersion: v1
kind: Service
metadata:
  name: {{ include "intellisper.fullname" . }}
  labels:
    {{- include "intellisper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: api
  selector:
    {{- include "intellisper.selectorLabels" . | nindent 4 }}
{{- if ne (.Values.workloadType | default "rollout") "statefulset" }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "intellisper.fullname" . }}-preview
  labels:
    {{- include "intellisper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: api
  selector:
    {{- include "intellisper.selectorLabels" . | nindent 4 }}
{{- end }}
